<h1><%= @board.name %></h1> 
    <% total_spent = [] %>
    <% @board.images.each do |image| %>
    <% total_spent << image.image_price %>
    <% end %>
<div class="row">
<h3>Vision Board Budget $<%=@board.budget %></h3> 
<h3>Remaining $<%= @board.budget - total_spent.to_a.sum{ |e| e.to_i } %> </h3>
  <div class="col-md-8 vision-board">
  <div class="gridster ready">
    <ul id="hello">
    <% @id_num = 1 %>
    <% @board.images.each do |image|%>  
     <li id="image<%= @id_num %>" data-row="1" data-col="1" data-sizex="1" data-sizey="1">
     <p style="position:absolute; bottom:0; margin-left:25%; padding:0 10px; color:blue; background-color:rgba(255,255,255,.3);"><%= image.image_price %></p>
       <%= link_to(image_tag(image.image_url.large), "#" , class: 'imageLink') %>
     </li>
     <% @id_num += 1 %>
    <% end %>
    </ul>
    </div>
  </div>
  <div class="col-md-4 vision-selection">
    <%= form_for [@board, @board.images.build] do |f| %>
      <div class="form-group">
        <%= f.label 'Price' %>
        <%= f.text_field :image_price %>
      </div>
      <div class="form-group">
        <%= f.label 'Select Image' %>
        <%= f.file_field :image_url%>
        <%= f.hidden_field :image_url_cache %>
      </div>
      <div class="form-group">
        <%= f.label 'Upload Image From a URL' %>
        <%= f.text_field :remote_image_url %>
      </div>
      <div class="form-group">
        <%= f.submit "Add Image", class: 'btn btn-success' %>
      </div>
      <% end %>
      <%= label_tag 'js-desires' , "Search for an Image:" %>
      <%= text_field_tag 'js-desires'%>
      <div id="previews"></div>
  </div> 